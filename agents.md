# Agent Instructions

## Build Verification

After making Kotlin code changes, always run the following command to verify compilation:

```bash
.\gradlew.bat compileKotlinWasmJs
```

This ensures there are no compile errors in the Kotlin/Wasm target before considering changes complete.
